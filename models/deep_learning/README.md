# Deep Learning Models

## Purpose
This directory contains the architecture, training scripts, and inference logic for the Adaptive Workload model.

## Structure
*   `architectures/`: Model definitions (e.g., LSTM, Transformer, CNN).
*   `train.py`: Training loop.
*   `inference.py`: Real-time inference simulation.

## Environment
*   (Specify if a separate Python environment/conda env is needed here, e.g., for PyTorch/CUDA support).
